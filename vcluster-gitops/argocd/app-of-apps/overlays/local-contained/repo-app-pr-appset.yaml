apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: "onyx-app-pr"
  namespace: argocd
spec:
  generators:
    - pullRequest:
        gitea:
          owner: "loft-demos"
          repo: "onyx-app"
          api: "https://github.com"
          tokenRef:
            secretName: loft-demo-org-cred
            key: token
        requeueAfterSeconds: 30
  template:
    metadata:
      name: "onyx-app-{{branch}}-pr{{number}}"
    spec:
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
      source:
        repoURL: "https://github.com/loft-demos/onyx-app.git"
        targetRevision: "{{head_sha}}"
        path: helm-chart/
        helm:
          parameters:
            - name: "image.repository"
              value: "ghcr.io/loft-demos/onyx-app-demo-app"
            - name: "image.tag"
              value: "{{head_short_sha}}"
            - name: "image.args.text"
              value: "Hello from onyx-app pr-{{number}} commit {{head_short_sha}}"
            - name: "ingress.hosts[0].host"
              value: "onyx-app-pr-{{number}}-onyx.us.demo.dev"
            - name: ingress.hosts[0].paths[0].backend.service.name
              value: juliet-app
            - name: ingress.hosts[0].paths[0].backend.service.port.name
              value: http
            - name: ingress.hosts[0].paths[0].path
              value: /
            - name: ingress.hosts[0].paths[0].pathType
              value: prefix
            - name: "ingress.tls[0].hosts[0]"
              value: "onyx-app-pr-{{number}}-onyx.us.demo.dev"
      project: "default"
      destination:
        server: "https://onyx.us.demo.dev/kubernetes/project/default/virtualcluster/onyx-app-pr-{{number}}"
        namespace: preview-hello-world-app
      info:
        - name: Preview App Link
          value: >-
            https://onyx-app-pr-{{number}}-onyx.us.demo.dev
        - name: Pull Request
          value: >-
            https://github.com/loft-demos/onyx-app/pulls/{{number}}
